[project]
name = "files"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { email = "bavlesamy@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.0",
    "scikit-learn>=1.4",
]

[tool.uv]
package = false
